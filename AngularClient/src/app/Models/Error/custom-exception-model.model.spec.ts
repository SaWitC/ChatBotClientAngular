import { CustomExceptionModel } from './custom-exception-model.model';

describe('CustomExceptionModel', () => {
  it('should create an instance', () => {
    expect(new CustomExceptionModel()).toBeTruthy();
  });
});
